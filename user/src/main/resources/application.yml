server:
  port: 8081

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_user
    username: root
    password: Ashit@1206


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
